flask==3.1.0
flask-cors==5.0.1
scikit-learn>=1.3.0
pandas>=2.0.0
numpy>=1.24.0
joblib>=1.3.0

# Production WSGI servers (use ONE depending on platform):
# Windows local dev / production:
waitress>=3.0.0
# Linux / Docker production (comment out waitress above if using gunicorn):
# gunicorn>=22.0.0
